# Meeting Audit Dashboard

Next.js App Router dashboard for reviewing recurring meeting load from the seeded CSV and dashboard-content JSON under `public/data/`.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run start`
- `npm run lint`

## Deployment

Set the Vercel project root to `web/`.
